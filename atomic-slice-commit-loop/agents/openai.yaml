interface:
  display_name: "Atomic Slice Loop"
  short_description: "Land approved work as validated commits"
  default_prompt: "Use $atomic-slice-commit-loop to implement the approved plan as the smallest independently valid commit sequence, committing each completed slice by default while preserving unrelated worktree changes."
