interface:
  display_name: "Preliminary Analysis"
  short_description: "Record verified run findings"
  default_prompt: "Use $preliminary-analysis-note to record the verified findings, provenance, status, and result-specific caveats from this output-bearing run or decision-bearing validation in its owning repository."
